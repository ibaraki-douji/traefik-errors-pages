{
  "name": "traefik-custom-errors",
  "version": "0.0.1",
  "description": "Create custom pages / redirections / reverse proxy in case of some errors",
  "main": "lib/main.js",
  "scripts": {
    "start": "node lib/main.js",
    "build": "tsc",
    "dev": "tsc -w",
    "start:dev": "nodemon lib/main.js",
    "parallel": "npm run build && npm-run-all --parallel dev start:dev"
  },
  "author": "Ibaraki Douji",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^20.14.2",
    "nodemon": "^3.1.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@ibaraki-douji/console": "^1.0.1",
    "dotenv": "^16.4.5"
  }
}
